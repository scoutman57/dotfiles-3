# .bashrc: executed in non-login shells
# Source: @mathiasbynens

# Here we simply source .bash_profile so that
# settings remain the same in login and non-login shells.
[ -n "$PS1" ] && source ~/.bash_profile;
