# .extra: any other commands you want to be sourced

# Load Docker on start
# UPDATE: commented out because not particularly reliable and slows down
# Terminal start time by a lot. Better to use the Docker Quickstart Terminal.
# docker-machine start &> /dev/null
# eval "$(docker-machine env default)"

# Load rbenv on start
eval "$(rbenv init -)"

# Load nvm on start
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
