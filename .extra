# .extra: any other commands you want to be sourced

# Load rbenv on start
eval "$(rbenv init -)"

# Figma: set Rack environment
export RACK_ENV=development

# Load nvm on start
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
